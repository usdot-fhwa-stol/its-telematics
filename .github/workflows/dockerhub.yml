name: Docker Hub build

on:
  push:
    branches:
      - "develop"
      - "master"
      - "release/*"
    tags:
      - "carma-system-*"

jobs:
  determine_docker_org_and_tag:
    runs-on: ubuntu-latest
    outputs:
      docker_organization: ${{ steps.docker-org-and-tag.outputs.docker_organization }}
      docker_image_tag: ${{ steps.docker-org-and-tag.outputs.docker_image_tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - id: docker-org-and-tag
        uses: usdot-fhwa-stol/actions/docker-org-and-tag@main

  determine_docker_image_metadata:
    runs-on: ubuntu-latest
    outputs:
      build_date: ${{ steps.build-metadata.outputs.build_date }}
      vcs_ref: ${{ steps.build-metadata.outputs.vcs_ref }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Determine Build Metadata
        id: build-metadata
        uses: usdot-fhwa-stol/actions/docker-build-metadata@main

  dockerhub-telematic-cloud-messaging:
    needs: [determine_docker_org_and_tag, determine_docker_image_metadata]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to DockerHub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: ./telematic_system/telematic_cloud_messaging/
          push: true
          sbom: true
          provenance: true
          tags: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_cloud_messaging:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          build-args: |
            BUILD_DATE=${{ needs.determine_docker_image_metadata.outputs.build_date }}
            VCS_REF=${{ needs.determine_docker_image_metadata.outputs.vcs_ref }}
      - name: Docker Scout Record
        if: ${{ github.ref_type == 'branch' }}
        uses: docker/scout-action@v1
        with:
          command: environment
          environment: ${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          image: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_cloud_messaging:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          organization: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}

  dockerhub-telematic-rsu-management-service:
    needs: [determine_docker_org_and_tag, determine_docker_image_metadata]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to DockerHub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: ./telematic_system/telematic_rsu_management_service/
          push: true
          sbom: true
          provenance: true
          tags: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_rsu_management_service:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          build-args: |
            BUILD_DATE=${{ needs.determine_docker_image_metadata.outputs.build_date }}
            VCS_REF=${{ needs.determine_docker_image_metadata.outputs.vcs_ref }}
      - name: Docker Scout Record
        if: ${{ github.ref_type == 'branch' }}
        uses: docker/scout-action@v1
        with:
          command: environment
          environment: ${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          image: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_rsu_management_service:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          organization: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}

  dockerhub-kafka-nats-bridge:
    needs: [determine_docker_org_and_tag, determine_docker_image_metadata]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to DockerHub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: ./telematic_system/telematic_units/kafka_bridge/
          push: true
          sbom: true
          provenance: true
          tags: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematics-kafka-nats-bridge:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          build-args: |
            BUILD_DATE=${{ needs.determine_docker_image_metadata.outputs.build_date }}
            VCS_REF=${{ needs.determine_docker_image_metadata.outputs.vcs_ref }}
      - name: Docker Scout Record
        if: ${{ github.ref_type == 'branch' }}
        uses: docker/scout-action@v1
        with:
          command: environment
          environment: ${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          image: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematics-kafka-nats-bridge:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          organization: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}

  dockerhub-carma-vehicle-bridge:
    needs: [determine_docker_org_and_tag, determine_docker_image_metadata]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to DockerHub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: ./telematic_system/telematic_units/carma_vehicle_bridge/
          push: true
          sbom: true
          provenance: true
          tags: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/carma_vehicle_nats_bridge:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          build-args: |
            BUILD_DATE=${{ needs.determine_docker_image_metadata.outputs.build_date }}
            VCS_REF=${{ needs.determine_docker_image_metadata.outputs.vcs_ref }}
      - name: Docker Scout Record
        if: ${{ github.ref_type == 'branch' }}
        uses: docker/scout-action@v1
        with:
          command: environment
          environment: ${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          image: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/carma_vehicle_nats_bridge:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          organization: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}

  dockerhub-carma-cloud-bridge:
    needs: [determine_docker_org_and_tag, determine_docker_image_metadata]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to DockerHub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: ./telematic_system/telematic_units/carma_cloud_bridge/
          push: true
          sbom: true
          provenance: true
          tags: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/carma_cloud_nats_bridge:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          build-args: |
            BUILD_DATE=${{ needs.determine_docker_image_metadata.outputs.build_date }}
            VCS_REF=${{ needs.determine_docker_image_metadata.outputs.vcs_ref }}
      - name: Docker Scout Record
        if: ${{ github.ref_type == 'branch' }}
        uses: docker/scout-action@v1
        with:
          command: environment
          environment: ${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          image: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/carma_cloud_nats_bridge:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          organization: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}

  dockerhub-telematic-historical-data-processing:
    needs: [determine_docker_org_and_tag, determine_docker_image_metadata]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to DockerHub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: ./telematic_system/telematic_historical_data_processing/
          push: true
          sbom: true
          provenance: true
          tags: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_historical_data_processing:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          build-args: |
            BUILD_DATE=${{ needs.determine_docker_image_metadata.outputs.build_date }}
            VCS_REF=${{ needs.determine_docker_image_metadata.outputs.vcs_ref }}
      - name: Docker Scout Record
        if: ${{ github.ref_type == 'branch' }}
        uses: docker/scout-action@v1
        with:
          command: environment
          environment: ${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          image: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_historical_data_processing:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          organization: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}

  dockerhub-telematic-web-server:
    needs: [determine_docker_org_and_tag, determine_docker_image_metadata]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to DockerHub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: ./telematic_system/telematic_apps/web_app/server
          push: true
          sbom: true
          provenance: true
          tags: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_web_server:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          build-args: |
            BUILD_DATE=${{ needs.determine_docker_image_metadata.outputs.build_date }}
            VCS_REF=${{ needs.determine_docker_image_metadata.outputs.vcs_ref }}
      - name: Docker Scout Record
        if: ${{ github.ref_type == 'branch' }}
        uses: docker/scout-action@v1
        with:
          command: environment
          environment: ${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          image: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_web_server:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          organization: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}

  dockerhub-telematic-web-client:
    needs: [determine_docker_org_and_tag, determine_docker_image_metadata]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to DockerHub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: ./telematic_system/telematic_apps/web_app/client
          push: true
          sbom: true
          provenance: true
          tags: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_web_client:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          build-args: |
            BUILD_DATE=${{ needs.determine_docker_image_metadata.outputs.build_date }}
            VCS_REF=${{ needs.determine_docker_image_metadata.outputs.vcs_ref }}
      - name: Docker Scout Record
        if: ${{ github.ref_type == 'branch' }}
        uses: docker/scout-action@v1
        with:
          command: environment
          environment: ${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          image: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_web_client:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          organization: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}

  dockerhub-telematic-apache2:
    needs: [determine_docker_org_and_tag, determine_docker_image_metadata]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to DockerHub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: ./telematic_system/telematic_apps/apache2
          push: true
          sbom: true
          provenance: true
          tags: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_apache2:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          build-args: |
            BUILD_DATE=${{ needs.determine_docker_image_metadata.outputs.build_date }}
            VCS_REF=${{ needs.determine_docker_image_metadata.outputs.vcs_ref }}
      - name: Docker Scout Record
        if: ${{ github.ref_type == 'branch' }}
        uses: docker/scout-action@v1
        with:
          command: environment
          environment: ${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          image: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_apache2:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          organization: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}

  dockerhub-telematic-grafana:
    needs: [determine_docker_org_and_tag, determine_docker_image_metadata]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to DockerHub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: ./telematic_system/telematic_apps/grafana
          push: true
          sbom: true
          provenance: true
          tags: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_grafana:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          build-args: |
            BUILD_DATE=${{ needs.determine_docker_image_metadata.outputs.build_date }}
            VCS_REF=${{ needs.determine_docker_image_metadata.outputs.vcs_ref }}
      - name: Docker Scout Record
        if: ${{ github.ref_type == 'branch' }}
        uses: docker/scout-action@v1
        with:
          command: environment
          environment: ${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          image: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_grafana:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          organization: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}

  dockerhub-telematic-local-apache2:
    needs: [determine_docker_org_and_tag, determine_docker_image_metadata]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to DockerHub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: ./telematic_system/telematic_apps/apache2
          file: ./telematic_system/telematic_apps/apache2/local.Dockerfile
          push: true
          sbom: true
          provenance: true
          tags: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_local_apache2:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          build-args: |
            BUILD_DATE=${{ needs.determine_docker_image_metadata.outputs.build_date }}
            VCS_REF=${{ needs.determine_docker_image_metadata.outputs.vcs_ref }}
      - name: Docker Scout Record
        if: ${{ github.ref_type == 'branch' }}
        uses: docker/scout-action@v1
        with:
          command: environment
          environment: ${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          image: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}/telematic_local_apache2:${{ needs.determine_docker_org_and_tag.outputs.docker_image_tag }}
          organization: ${{ needs.determine_docker_org_and_tag.outputs.docker_organization }}