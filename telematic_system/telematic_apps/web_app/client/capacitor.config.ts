import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.telematic.app',
  appName: 'telematic_mobile_app',
  webDir: 'build'
};

export default config;
